{
    "name": "blessed-tetris",
    "engines": {
        "node": ">=8"
    },
    "scripts": {
        "start": "node index",
        "daemon": "node daemon",
        "reload": "node daemon --reload",
        "serve-dev": "node index --serve-dev",
        "serve": "pm2 start --name \"tetris\" index.js -- --serve"
    },
    "dependencies": {
        "blessed": "^0.1.81",
        "telnet2": "^0.0.1"
    },
    "devDependencies": {
        "chokidar": "^1.7.0"
    }
}
